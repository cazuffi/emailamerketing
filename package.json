{
  "name": "email-marketing",
  "version": "2.0.0",
  "description": "D365-native HTML email studio — preview, build, paste into Insight Hub",
  "private": true,
  "scripts": {
    "build": "node scripts/build.js",
    "preview": "node scripts/preview.js"
  },
  "devDependencies": {
    "chokidar": "^3.6.0"
  }
}
